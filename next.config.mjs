/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Ensure that environment variables from .env are passed down correctly if needed
};

export default nextConfig;
